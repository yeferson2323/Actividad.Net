@model IEnumerable<RoarMot.Models.Compra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MetodoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AgregarMasArtiCulos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FacturaProducto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MetodoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AgregarMasArtiCulos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FacturaProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCompra">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCompra">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCompra">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
